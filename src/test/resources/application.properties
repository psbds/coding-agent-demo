# Test configuration
quarkus.rest-client.dolarapi-api.url=http://localhost:8089

# Disable Redis for unit tests - mocked in tests
quarkus.redis.devservices.enabled=false
quarkus.redis.hosts=redis://localhost:6379

# Cache TTL for testing
coding-agent.cache.eur-exchange.ttl-seconds=60

# Test logging
quarkus.log.level=INFO
quarkus.log.category."psbds.demo".level=DEBUG
